import { it } from "node:test"


describe('Install dependencies with the selected options form prompts', () => {
  it('should install dependencies with the selected options form prompts', () => {
    cy.visit('http://localhost:3000')
  })
})
